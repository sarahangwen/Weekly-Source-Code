//In JavaScript, an array is a special type of object used to store multiple values in a single variable.
 //Arrays are dynamic, meaning they can grow and shrink as needed, and they can hold different data types in the same array.
 // Creating an Array
//You can create an array in multiple ways:

// 1 Using Array Literals (Recommended)
let fruits = ["Apple", "Banana", "Mango"];

//Using the Array Constructor
let number1 = new Array(1, 2, 3, 4, 5)


//Array Methods
 //Adding and Removing Elements
//.push(value) → Adds to the end
//.pop() → Removes the last element
//.unshift(value) → Adds to the beginning
//.shift() → Removes the first element

//Examples
// Create an array
let fruits1 = ["Apple", "Banana", "Cherry"];
// Print array in the terminal
console.log(fruits1);


/*Accessing Array Elements
javascript
Copy
Edit
let colors = ["Red", "Blue", "Green"];
console.log(colors[0]); // Red
console.log(colors[1]); // Blue
console.log(colors[2]); // Green*/



// Accessing arrays by Elements
let colors = ["Red", "Blue", "Green"];
console.log(colors[0]); // Red
console.log(colors[1]); // Blue
console.log(colors[2]); // Green






